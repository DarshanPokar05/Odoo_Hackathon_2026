{
  "name": "assetflow-monorepo",
  "version": "1.0.0",
  "private": true,
  "description": "AssetFlow - Enterprise Asset & Resource Management System",
  "scripts": {
    "dev": "concurrently --kill-others-on-fail \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "dev:backend": "npm run dev --prefix backend",
    "dev:frontend": "npm run dev --prefix frontend",
    "install:all": "npm install && npm --prefix backend install && npm --prefix frontend install",
    "migrate": "npm run migrate --prefix backend",
    "seed": "npm run seed --prefix backend",
    "test:constraints": "npm run test:constraints --prefix backend"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
